query getShelf($id: ID!) {
  getShelf(id: $id) {
    shelf {
      id
      title
      description
      container {
        id
        title
      }
      items {
        id
        title
      }
      createdAt
      updatedAt
    }
  }
}
